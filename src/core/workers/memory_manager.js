function allocate_memory(void * ptr, size_t size) {
  //TODO: implement this function
  // return Buffer.alloc(size);
}

function free_memory(void * ptr) {
  //TODO: implement this function
  // return Buffer.free(ptr);
}